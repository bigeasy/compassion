const compassion = new Compassion(
class Consensus {
    constructor () {
    }
    bootstrap: () => this._table.bootstrap(event.self.arrived, this._count),
    join: async (event, shifter) => this._table.join(event.self.arrived, await shifter.shift()),
    arrive: async (event) => {
        await service.fastify.listen(0)
        this._cubbyholes.resolve(event.government.promise, this._table.getSnapshot())
        this._table.arrive(event.government.promise, event.entry.arrive.promise)
    },
    acclimated: () => {
        this._cubbyholes.remove(envelope.government.promise)
    },
    depart: () => {
        this._cubbyholes.remove(envelope.government.promise)
    }
})

await compassion.listen(0)
await mingle.listen(0)
