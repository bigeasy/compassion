sudo: false

language: node_js

node_js:
#  - '4'
#  - '6'
#  - '7'
  - '8'

branches:
  only:
    - master
    - travis-ci

before_install:
  - for dir in compassion.*; do (cd $dir && npm install); done
  - git checkout .
  - npm install istanbul coveralls
