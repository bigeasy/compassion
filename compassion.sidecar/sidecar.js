var cadence = require('cadence')
var Local = require('./middleware/local')
var Networked = require('./middleware/networked')

function Sidecar (options) {
}

module.exports = cadence(function (async, options) {
})
